# 变更记录

## Bitget 现货 5U 实盘灰度候选实现
日期：2026-06-02

范围：`Bitget Spot / BTCUSDT / 4h` live-only 小额灰度，不复用 Binance 测试网闭环，不启用无界 live loop。

已完成：

- 新增 Bitget 现货凭证配置：`KXIAN_BITGET_API_KEY`、`KXIAN_BITGET_API_SECRET`、`KXIAN_BITGET_API_PASSPHRASE`。
- 将交易所分支扩展为显式 `binance / okx / bitget`，避免未知交易所误落到 Bitget 私有接口。
- 新增 Bitget 公有行情、交易规则拉取、签名请求、下单、查单、撤单、账户余额和成交同步适配。
- Bitget `mode=testnet` 保持阻断；Bitget 只走 live-only 灰度，不冒充测试网。
- 新增 `approve-bitget-live-gray`，写入带 `approved_at` 和 `approval_id` 的 Bitget 灰度批准证据。
- Bitget live 灰度强制 `KXIAN_MAX_LIVE_ORDER_USDT<=5`，`trade-loop` 只能 `--max-iterations 1`，并阻断 `test-order` 和 `run-once`。
- 最终 `launch-checklist --target live` 只接受批准时间之后的 Bitget canary 订单证据，旧订单不能冒充新验收。
- Bitget 签名请求 JSON 输出会脱敏 API key、passphrase、signature 和 signature payload。
- 新增中文手册 `docs/Bitget实盘灰度手册.md`，固定只读检查、人工确认、单轮 canary、查单/撤单/成交同步、账户同步和最终 checklist 顺序。

保持不变：

- 不读取、不回显、不提交真实 `.env` 密钥。
- 不执行无界 live loop。
- 不把 Bitget 包装成 Binance Spot Testnet。

## Bitget 只读策略研究与 2h 行情适配

日期：2026-06-02

范围：继续推进 `Bitget Spot / BTCUSDT` 只读策略证据研究，不进入 5U canary。

新增：

- Bitget 公开行情新增 `2h` 本地合成支持：系统从 Bitget 官方支持的 `1h` 现货历史 K 线拉取数据，再本地聚合为 `2h`，避免直接请求 Bitget 不支持的 `2h` granularity。
- 只读研究类 CLI 命令使用非执行配置校验，避免临时研究 `1h/2h` 时被 live confirmation 短语误卡。
- 带 `--promote` 的研究命令继续使用严格配置校验，防止写入 active profile 时绕过 live confirmation。
- `--promote` 写入 profile 前会按最终目标 `exchange/symbol/interval` 再次校验 live confirmation，防止用 `4h` 确认短语写入 `1h/2h` profile。
- `approve-bitget-live-gray`、`promote-profile-to-live`、`promote-profile-to-testnet` 不再走 relaxed 配置加载，继续保留执行级严格校验；`run-once`、`trade-loop` 保留结构化 launch gate 阻断输出，不放松后续执行门禁。
- 补充 `docs/Bitget策略证据研究报告.md`：记录 4h/2h/1h 均未找到跨 5 段样本通过硬门禁的候选。
- 新增测试覆盖 Bitget 2h 行情本地聚合、只读研究 relaxed 配置、`--promote` 严格配置。

当前结论：

- 4h 主线没有可晋级候选。
- 2h 研究样本 5 段、10572 根 K 线，有效预筛候选通过数为 0。
- 1h 研究样本 5 段、21144 根 K 线，有效预筛候选通过数为 0。
- 当前必须继续停在 `blocked_before_bitget_live_canary`，不得写入 live profile，不得执行 canary 或真实下单。

## 测试网闭环候选版本

日期：2026-06-01

范围：`Binance testnet / BTCUSDT / 4h` 测试网闭环候选。

已完成：

- 初始化 Git 基线，并确认 `.gitignore` 覆盖 `.env`、`data/`、`*.sqlite3` 等敏感运行产物。
- 增强 `testnet-dry-run` 输出，固定返回配置摘要、profile、凭证 boolean、账户同步、fill 同步和订单生命周期，不回显 API Key/Secret。
- 增强 `testnet-observe` 证据，记录最新订单生命周期，避免 bounded 观察只凭无 error 通过。
- 强化 `launch-checklist --target testnet`，当 bounded 观察存在 open order 或生命周期不可接受时保持 blocked，并给出清理步骤。
- 仪表盘加入统一按钮反馈 helper，覆盖 loading、disabled、`aria-busy`、成功/失败 toast 和失败恢复。
- 仪表盘测试网区域固定展示 dry-run/observe 状态、周期成功失败数、失败原因和订单闭环状态。
- 新增中文操作手册 `docs/测试网闭环操作手册.md`，明确只停在测试网闭环，不进入 live。

未纳入本候选版本：

- 不新增完整 `testnet-close-loop` 编排命令。
- 不执行 `promote-profile-to-live`。
- 不创建、不覆盖、不读取真实 `.env` 密钥。
- 不进行真实 live 下单。

## 测试网闭环验收增强

日期：2026-06-01

范围：继续围绕 `Binance Spot Testnet / BTCUSDT / 4h`，把已经跑通的测试网人工验收固化为可复现、可审计、可下载的交付材料。

新增：

- 新增中文验收记录 `docs/测试网闭环验收记录.md`，记录非下单 6 轮、bounded 6 轮、测试网账户清理订单和最终 `phase=testnet_observed_ready_for_live_review`。
- 新增脱敏证据包导出 helper，`testnet-dry-run`、`testnet-observe`、`launch-checklist --target testnet` 可使用 `--evidence-out` 写入 JSON 证据。
- 新增 `testnet-close-loop` 编排命令；默认停在 bounded 确认前，只有显式 `--confirm-bounded-testnet-order` 且测试网自动交易开关已启用时才进入 bounded 观察。
- Dashboard 新增测试网验收步骤条和 `下载证据` 按钮，证据下载走只读 API 与统一按钮反馈。
- CI 增加基础 pytest、compileall 和离线 paper dry-run smoke。

保持不变：

- 不执行 `promote-profile-to-live`。
- 不启动 live。
- 不读取或回显 `.env` 中 API key/secret 原文或片段。
