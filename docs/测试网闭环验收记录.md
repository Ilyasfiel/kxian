# 测试网闭环验收记录

日期：2026-06-01

交付基线：`b4dfe76 feat: complete testnet close loop`

验收范围：`Binance Spot Testnet / BTCUSDT / 4h`

## 验收结论

本轮人工验收已完成测试网闭环，最终 `kxian-bot launch-checklist --target testnet` 返回：

```text
status=pass
phase=testnet_observed_ready_for_live_review
```

到此停止。未执行 `promote-profile-to-live`，未启动 live，未执行 live checklist 或 live loop。

## 关键证据

- 非下单观察：观察 ID `88878706-3996-4c69-876a-2275612d4f2d`，完成 6 轮，失败 0 次。
- Bounded 观察：观察 ID `332ce95c-dcc2-4e8b-9f87-b335f72db2db`，完成 6 轮，失败 0 次。
- Bounded 结果：健康 idle 可接受，`open_order_count=0`，账户同步成功，fill 同步成功。
- 最终 checklist：`status=pass`，`phase=testnet_observed_ready_for_live_review`。

## 测试网账户清理

验收前 Binance Spot Testnet 账户存在 1 BTC。为了让测试网账户恢复到只保留 USDT 的验收起点，执行了一笔测试网卖单：

```text
symbol=BTCUSDT
side=sell
quantity=1
price=73322.45
exchange_order_id=<测试网订单号已脱敏>
status=filled
```

这是 sandbox 测试网账户清理动作，不是真实资金交易，也不是实盘下单证据。清理后重新执行 `sync-fills`、`account-balance`、`order-status` 和 `preflight`，确认 BTC 余额为 0、订单状态已成交、启动门禁通过。

## 停止边界

- 不使用生产 API key。
- 不提交 `.env`。
- 不写入或回显 API key/secret 原文或片段。
- 不执行实盘晋升。
- 不启动 live 自动交易。
