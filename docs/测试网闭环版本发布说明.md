# 测试网闭环版本发布说明

发布日期：2026-06-01

版本标签：`v0.1.0-testnet`

提交：`0c97babc2a2448e9f49dfc0108f83bbd4f81add6`

提交说明：`feat: 完成测试网闭环验收与证据导出`

## 发布结论

本版本完成 `Binance Spot Testnet / BTCUSDT / 4h` 测试网闭环验收，并停止在测试网复核边界。最终验收标准为：

```text
status=pass
phase=testnet_observed_ready_for_live_review
```

该标签只表示测试网闭环版本，不表示实盘可直接启动，也不表示已完成 live 审批。

## 验收范围

- 固定范围：`testnet / binance / BTCUSDT / 4h`
- 固定端点：Binance Spot Testnet 交易端点
- 非下单观察：6 轮完成，失败 0 次
- Bounded 观察：6 轮完成，失败 0 次，健康 idle 可接受
- 最终订单状态：`open_order_count=0`
- 账户同步：通过
- fill 同步：通过
- 最终 checklist：`status=pass`，`phase=testnet_observed_ready_for_live_review`

## 发布内容

- 新增测试网证据包导出能力，支持 `--evidence-out`。
- 新增测试网闭环安全门禁；`testnet-close-loop` 编排命令保留为后续增强方向。
- Dashboard 增加测试网验收时间线、证据下载、固定失败原因展示和按钮反馈。
- 新增 CI，固定离线 paper 环境运行 compile、pytest 和 smoke。
- 新增中文操作手册、验收记录和本发布说明。

## 安全边界

- 不执行 `promote-profile-to-live`。
- 不启动 live loop。
- 不执行 `launch-checklist --target live` 作为本阶段验收。
- 不读取、回显或提交 `.env` 中的真实 API key/secret。
- 不提交 `data/`、`artifacts/`、`*.sqlite3`。
- CI 中强制关闭 live 和测试网自动交易，只运行离线测试与 paper smoke。

## 远端与 CI

- GitHub 远端：`https://github.com/Ilyasfiel/kxian.git`
- Gitee 远端：`https://gitee.com/neuronray/kxian.git`
- GitHub tag：`v0.1.0-testnet` 已同步。
- Gitee tag：`v0.1.0-testnet` 已同步。
- GitHub CI：`CI #2` 对 `v0.1.0-testnet / 0c97bab` 为 `Success`。
- Gitee：已确认分支和 tag 同步；如后续启用 Gitee 流水线，再补充流水线结果。

## 已知限制

- 本版本不包含完整实盘审批流程。
- 本版本不做生产 API key 验证。
- Dashboard 浏览器测试使用本地 mock 接口，不连接 Binance，也不触发真实测试网订单。
- 异常订单恢复仍按中文操作手册执行，暂未做自动恢复编排。

## 下一阶段

- 完善测试网证据包字段校验与审计说明。
- 持续扩展 Dashboard 浏览器级交互回归测试。
- 将“允许继续 / 必须中止 / 下一步动作”整理成测试网判定表。
- 后续再评估 secret scan、不可变审计导出、告警、OCO/保护单沙箱演练。
